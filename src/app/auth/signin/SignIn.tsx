import React from 'react';
import SigninForm from '@/components/auth/signInForm';

const Signin = () => {
    return (
        <>
            <SigninForm />
        </>
    );
};

export default Signin;
